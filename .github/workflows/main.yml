name: Release

on:
  push:
    tags: # 当推送新标签时触发
      - 'v[0-9]+.[0-9]+.[0-9]+' # 匹配类似 v1.0.0 的标签格式

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build & Test
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    
    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        submodules: true

    - name: Setup Rust Toolchain
      uses: dtolnay/rust-toolchain@master
      with:
        toolchain: stable
        components: rustfmt, clippy

    - name: Build (Release)
      run: cargo build --release --verbose
      
    - name: Run tests
      run: cargo test --verbose

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.os }}-build
        path: target/release/
  
  release:
    name: Create Release
    needs: build # 确保在 build job 完成后才运行
    runs-on: ubuntu-latest

    permissions:
      contents: write # 👈 在这里添加

    steps:
    - name: Get tag
      id: get_tag
      run: echo "TAG_VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV

    - name: Get the SHA of the tag
      id: get_sha
      run: echo "TAG_SHA=$(git rev-parse HEAD)" >> $GITHUB_ENV

    - name: Create Release
      id: create_release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ env.TAG_VERSION }}
        name: Release ${{ env.TAG_VERSION }}
        draft: false
        prerelease: false
        body: |
          ### New Features
          - (Write your release notes here)
          
          ### Bug Fixes
          - (Write your bug fixes here)
        
    - name: Download Artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./artifacts

    - name: Upload Linux Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./artifacts/ubuntu-latest-build/RustF # 更改为你的可执行文件名
        asset_name: RustF-linux
        asset_content_type: application/octet-stream
        
    - name: Upload macOS Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./artifacts/macos-latest-build/RustF # 更改为你的可执行文件名
        asset_name: RustF-macos
        asset_content_type: application/octet-stream

    - name: Upload Windows Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./artifacts/windows-latest-build/RustF.exe # 更改为你的可执行文件名
        asset_name: RustF-windows.exe
        asset_content_type: application/octet-stream
